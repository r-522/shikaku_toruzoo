# 07 納品手順書

## 1. 概要

### 1.1 目的
資格取るぞー！アプリ（shikaku_toruzoo）を Google Cloud Run へデプロイするための手順を記載する。

### 1.2 デプロイ構成

```
┌─────────────────────────────────────────────────────┐
│                 Google Cloud Run                     │
│                                                      │
│  ┌───────────────────────────────────────────────┐  │
│  │        shikaku_toruzoo コンテナ                │  │
│  │                                               │  │
│  │  Actix-web (Rust)                             │  │
│  │  ・API エンドポイント (/api/*)                  │  │
│  │  ・静的ファイル配信 (Vue ビルド成果物)           │  │
│  │  ・SPA フォールバック                           │  │
│  └───────────────────────────────────────────────┘  │
│                       │ HTTPS (マネージド証明書)       │
└───────────────────────┼─────────────────────────────┘
                        ▼
              ┌─────────────────────┐
              │     Supabase        │
              │   (PostgreSQL)      │
              └─────────────────────┘
```

### 1.3 前提条件

| 項目 | 要件 |
|------|------|
| Google Cloud アカウント | プロジェクト作成済み、課金有効 |
| gcloud CLI | v450.0.0 以上 |
| Docker | v24.0 以上 |
| Supabase | プロジェクト作成済み、REST API URL と service_role key 取得済み |

---

## 2. 事前準備

### 2.1 Google Cloud プロジェクトの設定

```bash
gcloud auth login

export PROJECT_ID="your-project-id"
gcloud config set project $PROJECT_ID

gcloud services enable \
  run.googleapis.com \
  containerregistry.googleapis.com \
  artifactregistry.googleapis.com \
  cloudbuild.googleapis.com

gcloud artifacts repositories create shikaku-toruzoo-repo \
  --repository-format=docker \
  --location=asia-northeast1 \
  --description="shikaku_toruzoo Docker images"
```

### 2.2 Supabase データベースの初期化

Supabase 管理画面の SQL Editor で以下を順番に実行する。

#### 2.2.1 拡張機能の有効化

```sql
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";
```

#### 2.2.2 テーブル作成

```sql
-- TBL_USER
CREATE TABLE "TBL_USER" (
    useid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    usenm VARCHAR(30) NOT NULL,
    useml VARCHAR(128) NOT NULL UNIQUE,
    usepw VARCHAR(256) NOT NULL,
    useca TIMESTAMPTZ NOT NULL DEFAULT now(),
    useua TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- TBL_MASTER
CREATE TABLE "TBL_MASTER" (
    masid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    masnm VARCHAR(200) NOT NULL,
    masct VARCHAR(30) NOT NULL DEFAULT 'その他',
    masnr VARCHAR(200) NOT NULL UNIQUE,
    masca TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- TBL_HOLDING
CREATE TABLE "TBL_HOLDING" (
    holid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    holui UUID NOT NULL REFERENCES "TBL_USER"(useid) ON DELETE CASCADE,
    holmi UUID NOT NULL REFERENCES "TBL_MASTER"(masid),
    holdt DATE,
    holca TIMESTAMPTZ NOT NULL DEFAULT now(),
    holua TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- TBL_GOAL
CREATE TABLE "TBL_GOAL" (
    goaid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    goaui UUID NOT NULL REFERENCES "TBL_USER"(useid) ON DELETE CASCADE,
    goami UUID NOT NULL REFERENCES "TBL_MASTER"(masid),
    goatd DATE NOT NULL,
    goast VARCHAR(20) NOT NULL DEFAULT 'exam_date'
        CHECK (goast IN ('exam_date', 'passed', 'failed', 'abandoned')),
    goamm TEXT,
    goash NUMERIC(6,1) NOT NULL DEFAULT 0,
    goaca TIMESTAMPTZ NOT NULL DEFAULT now(),
    goaua TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- TBL_FAVORITE
CREATE TABLE "TBL_FAVORITE" (
    favid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    favui UUID NOT NULL REFERENCES "TBL_USER"(useid) ON DELETE CASCADE,
    favti UUID NOT NULL REFERENCES "TBL_USER"(useid) ON DELETE CASCADE,
    favca TIMESTAMPTZ NOT NULL DEFAULT now(),
    UNIQUE (favui, favti),
    CHECK (favui != favti)
);

-- TBL_SESSION
CREATE TABLE "TBL_SESSION" (
    sesid UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    sesui UUID NOT NULL REFERENCES "TBL_USER"(useid) ON DELETE CASCADE,
    sestk VARCHAR(128) NOT NULL UNIQUE,
    sesea TIMESTAMPTZ NOT NULL,
    sesca TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

#### 2.2.3 インデックス作成

```sql
CREATE INDEX idx_holding_user ON "TBL_HOLDING"(holui);
CREATE INDEX idx_goal_user ON "TBL_GOAL"(goaui);
CREATE INDEX idx_goal_status ON "TBL_GOAL"(goast);
CREATE INDEX idx_fav_user ON "TBL_FAVORITE"(favui);
CREATE INDEX idx_session_expiry ON "TBL_SESSION"(sesea);
CREATE INDEX idx_master_name_trgm ON "TBL_MASTER" USING GIN (masnm gin_trgm_ops);
```

#### 2.2.4 更新トリガー作成

```sql
CREATE OR REPLACE FUNCTION update_user_updated_at()
RETURNS TRIGGER AS $$
BEGIN NEW.useua = now(); RETURN NEW; END;
$$ LANGUAGE plpgsql;
CREATE TRIGGER trg_user_updated BEFORE UPDATE ON "TBL_USER"
FOR EACH ROW EXECUTE FUNCTION update_user_updated_at();

CREATE OR REPLACE FUNCTION update_holding_updated_at()
RETURNS TRIGGER AS $$
BEGIN NEW.holua = now(); RETURN NEW; END;
$$ LANGUAGE plpgsql;
CREATE TRIGGER trg_holding_updated BEFORE UPDATE ON "TBL_HOLDING"
FOR EACH ROW EXECUTE FUNCTION update_holding_updated_at();

CREATE OR REPLACE FUNCTION update_goal_updated_at()
RETURNS TRIGGER AS $$
BEGIN NEW.goaua = now(); RETURN NEW; END;
$$ LANGUAGE plpgsql;
CREATE TRIGGER trg_goal_updated BEFORE UPDATE ON "TBL_GOAL"
FOR EACH ROW EXECUTE FUNCTION update_goal_updated_at();
```

#### 2.2.5 RLS の設定

```sql
ALTER TABLE "TBL_USER" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "TBL_MASTER" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "TBL_HOLDING" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "TBL_GOAL" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "TBL_FAVORITE" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "TBL_SESSION" ENABLE ROW LEVEL SECURITY;
-- service_role key を使う Rust バックエンドは RLS をバイパスする
```

> **重要**: `SUPABASE_KEY` には anon key ではなく `service_role` キーを使用すること。

#### 2.2.6 初期マスタデータ投入

```sql
INSERT INTO "TBL_MASTER" (masnm, masct, masnr) VALUES
('基本情報技術者', 'IT', '基本情報技術者'),
('応用情報技術者', 'IT', '応用情報技術者'),
('情報セキュリティマネジメント', 'IT', '情報セキュリティマネジメント'),
('AWS Solutions Architect Associate', 'IT', 'awssolutionsarchitectassociate'),
('TOEIC', '語学', 'toeic'),
('TOEFL iBT', '語学', 'toeflibt'),
('英検1級', '語学', '英検1級'),
('英検準1級', '語学', '英検準1級'),
('日商簿記2級', '金融', '日商簿記2級'),
('日商簿記3級', '金融', '日商簿記3級'),
('FP2級', '金融', 'fp2級'),
('FP3級', '金融', 'fp3級'),
('宅地建物取引士', '法律', '宅地建物取引士'),
('行政書士', '法律', '行政書士');
```

### 2.3 環境変数の準備

| 変数名 | 取得方法 |
|--------|---------|
| SUPABASE_URL | Supabase管理画面 > Settings > API > Project URL |
| SUPABASE_KEY | Supabase管理画面 > Settings > API > service_role key |
| EMAIL_HMAC_SECRET | `openssl rand -hex 32` で生成（変更不可） |
| CORS_ORIGIN | Cloud Run サービスURL（初回デプロイ後に確認） |

```bash
# EMAIL_HMAC_SECRET の生成
openssl rand -hex 32
```

---

## 3. ビルド手順

### 3.1 リポジトリのクローン

```bash
git clone https://github.com/r-522/shikaku_toruzoo.git
cd shikaku_toruzoo
```

### 3.2 Docker イメージのビルド

```bash
docker build -t shikaku-toruzoo:latest .
```

### 3.3 ローカル動作確認（任意）

```bash
docker run -p 8080:8080 \
  -e SUPABASE_URL="https://xxx.supabase.co" \
  -e SUPABASE_KEY="your-service-role-key" \
  -e EMAIL_HMAC_SECRET="your-hmac-secret" \
  -e CORS_ORIGIN="http://localhost:8080" \
  shikaku-toruzoo:latest
# ブラウザで http://localhost:8080 を確認
```

---

## 4. デプロイ手順

### 4.1 Artifact Registry へのプッシュ

```bash
gcloud auth configure-docker asia-northeast1-docker.pkg.dev

export IMAGE_TAG="asia-northeast1-docker.pkg.dev/$PROJECT_ID/shikaku-toruzoo-repo/shikaku-toruzoo:v1.0.0"
docker tag shikaku-toruzoo:latest $IMAGE_TAG
docker push $IMAGE_TAG
```

### 4.2 Cloud Run へのデプロイ

```bash
gcloud run deploy shikaku-toruzoo \
  --image $IMAGE_TAG \
  --region asia-northeast1 \
  --platform managed \
  --allow-unauthenticated \
  --port 8080 \
  --memory 512Mi \
  --cpu 1 \
  --min-instances 1 \
  --max-instances 10 \
  --timeout 300 \
  --set-env-vars "\
SUPABASE_URL=https://xxx.supabase.co,\
SUPABASE_KEY=your-service-role-key,\
EMAIL_HMAC_SECRET=your-hmac-secret,\
CORS_ORIGIN=https://dummy-url.run.app,\
RUST_LOG=info"
```

### 4.3 CORS_ORIGIN の更新（初回デプロイ後）

```bash
# サービスURLの確認
gcloud run services describe shikaku-toruzoo \
  --region asia-northeast1 \
  --format "value(status.url)"

# CORS_ORIGIN を実際の URL に更新
gcloud run services update shikaku-toruzoo \
  --region asia-northeast1 \
  --set-env-vars "CORS_ORIGIN=https://shikaku-toruzoo-xxxx-an.a.run.app"
```

### 4.4 デプロイ後の動作確認

```bash
export SERVICE_URL=$(gcloud run services describe shikaku-toruzoo \
  --region asia-northeast1 --format "value(status.url)")

# フロントエンド配信確認
curl -s -o /dev/null -w "%{http_code}" $SERVICE_URL
# → 200

# API 確認（未認証）
curl -s -o /dev/null -w "%{http_code}" $SERVICE_URL/api/auth/me
# → 401

# サインアップテスト
curl -X POST $SERVICE_URL/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"username":"テスト","email":"test@example.com","password":"TestPass1!"}'
# → 201
```

---

## 5. 運用設定

### 5.1 ログの確認

```bash
gcloud logging read \
  "resource.type=cloud_run_revision AND resource.labels.service_name=shikaku-toruzoo" \
  --limit 50

# リアルタイム
gcloud alpha run services logs tail shikaku-toruzoo --region asia-northeast1
```

### 5.2 セッションクリーンアップ（推奨）

Supabase SQL Editor で実行:

```sql
CREATE EXTENSION IF NOT EXISTS pg_cron;

SELECT cron.schedule(
    'cleanup-expired-sessions',
    '0 3 * * *',
    $$DELETE FROM "TBL_SESSION" WHERE sesea < now()$$
);
```

---

## 6. バージョンアップ手順

```bash
git pull origin main

export NEW_VERSION="v1.1.0"
docker build -t shikaku-toruzoo:$NEW_VERSION .

export NEW_IMAGE_TAG="asia-northeast1-docker.pkg.dev/$PROJECT_ID/shikaku-toruzoo-repo/shikaku-toruzoo:$NEW_VERSION"
docker tag shikaku-toruzoo:$NEW_VERSION $NEW_IMAGE_TAG
docker push $NEW_IMAGE_TAG

gcloud run deploy shikaku-toruzoo \
  --image $NEW_IMAGE_TAG \
  --region asia-northeast1
```

### ロールバック

```bash
gcloud run revisions list --service shikaku-toruzoo --region asia-northeast1

gcloud run services update-traffic shikaku-toruzoo \
  --region asia-northeast1 \
  --to-revisions REVISION_NAME=100
```

---

## 7. トラブルシューティング

| 問題 | 原因 | 対処法 |
|------|------|--------|
| コンテナ起動しない | 環境変数の不足 | `gcloud run services describe` で環境変数確認 |
| 502 Bad Gateway | 8080ポートでリッスンしていない | Dockerfile の EXPOSE/CMD を確認 |
| CORS エラー | CORS_ORIGIN が不正 | Cloud Run の URL と CORS_ORIGIN を照合 |
| DB 接続エラー | SUPABASE_URL / KEY が不正 | Supabase 管理画面で再取得 |
| サインインできない | EMAIL_HMAC_SECRET が変更された | シークレットキーは変更不可（全ユーザーのハッシュが無効化される） |
| 500エラー（signup） | SUPABASE_URL に /rest/v1 が必要 | db.rs の自動正規化を確認 |

---

## 8. チェックリスト

### デプロイ前

- [ ] Supabase テーブル・インデックス・トリガーが作成済み
- [ ] RLS が有効化済み
- [ ] 初期マスタデータが投入済み
- [ ] `EMAIL_HMAC_SECRET` が生成・保管済み（変更禁止）
- [ ] `service_role` キーを取得済み
- [ ] Docker イメージのビルドが成功
- [ ] ローカル動作確認済み

### デプロイ後

- [ ] ブラウザでサインアップ・サインインが正常に動作
- [ ] Cookie に HttpOnly, Secure, SameSite=Strict が設定
- [ ] 所持資格の CRUD が正常動作
- [ ] 目標の登録・勉強時間記録が正常動作
- [ ] コミュニティに他ユーザーが表示
- [ ] お気に入り機能が正常動作
- [ ] CORS_ORIGIN が正しいサービス URL に設定済み
- [ ] HTTPS で配信（ブラウザの鍵マーク確認）
- [ ] ソースマップが非公開
